<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfico HTTP en claro únicamente contra los hosts usados durante
  el desarrollo local. Cualquier otro destino (producción, IPs públicas)
  debe ir por HTTPS — TLS es obligatorio por defecto.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>      <!-- emulador Android -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
